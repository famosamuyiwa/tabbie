export enum ResponseStatus {
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
}

export enum QueryBy {
  EMAIL = 'email',
  USERNAME = 'username',
  BOTH = 'both',
}

export enum QueryAction {
  ADD = 'ADD',
  REMOVE = 'REMOVE',
}

export enum OAuthProvider {
  GOOGLE = 'GOOGLE',
}

export enum PaymentStatus {
  PAID = 'PAID',
  UNPAID = 'UNPAID',
  PENDING = 'PENDING',
}
