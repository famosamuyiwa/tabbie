import { GoogleOauthGuardsGuard } from './google-oauth.guards.guard';

describe('GoogleOauthGuardsGuard', () => {
  it('should be defined', () => {
    expect(new GoogleOauthGuardsGuard()).toBeDefined();
  });
});
