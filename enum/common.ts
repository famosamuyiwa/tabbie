export enum ResponseStatus {
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
}

export enum QueryBy {
  EMAIL = 'email',
  USERNAME = 'username',
  BOTH = 'both',
}

export enum OAuthProvider {
  GOOGLE = 'GOOGLE',
}
